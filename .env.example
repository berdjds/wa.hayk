# Database
DATABASE_URL="file:./dev.db"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-me-in-production-min-32-characters"

# Default admin credentials (seeded on first run)
ADMIN_EMAIL="admin@example.com"
ADMIN_PASSWORD="admin123"

# Optional: path to system Chromium when running in Docker
# PUPPETEER_EXECUTABLE_PATH="/usr/bin/chromium"

# Travel module: outbound email for workflow notifications.
# Leave SMTP_HOST unset to disable email delivery (deliveries are flagged
# FAILED and visible in /travel notifications; WhatsApp channel unaffected).
# SMTP_HOST="smtp.example.com"
# SMTP_PORT="587"
# SMTP_USER=""
# SMTP_PASS=""
# SMTP_FROM="WAControl Travel <travel@example.com>"

# Optional: separate browser binary for PDF rendering when puppeteer's bundled
# Chromium is broken on the host but WhatsApp must keep its own executable.
# TRAVEL_PDF_EXECUTABLE_PATH="/Applications/Google Chrome.app/Contents/MacOS/Google Chrome"
